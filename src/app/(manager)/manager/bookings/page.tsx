'use client'

import ListBookings from "@/components/bookings/ListBookings"

const Bookings = () => {

    return (
        <main>
            <ListBookings />
        </main>
    )
}

export default Bookings