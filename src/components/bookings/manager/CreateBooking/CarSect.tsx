'use client'

import { CarI } from "@/interface/api/car"

const CarSect = ({ car }:{car: CarI}) => {
    return (
        <section className="display">
            <h1>Car Section</h1>
        </section>
    )
}

export default CarSect